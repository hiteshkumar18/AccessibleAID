# EnableCare AI Backend — Python dependencies
#
# Install with:  pip install -r requirements.txt
# For Mac (Apple Silicon MPS acceleration):
#   pip install torch torchvision torchaudio
# For NVIDIA GPU:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121

fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0

# Core ML
torch>=2.2.0
torchvision>=0.17.0
torchaudio>=2.2.0

# Hugging Face (VLM, Whisper, Depth, Summarizer, Embeddings)
transformers>=4.41.0
accelerate>=0.30.0
sentencepiece>=0.2.0
tokenizers>=0.19.0
datasets>=2.19.0

# Sentence embeddings
sentence-transformers>=3.0.0

# Object detection (YOLOv8)
ultralytics>=8.2.0

# OCR
easyocr>=1.7.0
opencv-python-headless>=4.9.0

# Image processing
Pillow>=10.3.0
numpy>=1.26.0

# Optional: faster image loading
# imageio>=2.34.0
